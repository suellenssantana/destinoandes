import Link from "next/link";
import { Footer, Header } from "../components";
import { posts } from "./data";
export default function Blog(){const [featured,...rest]=posts;return <><Header/><main><section className="inner-hero blog-hero"><p className="kicker light">GUIAS DESTINO ANDES</p><h1>Viaje com<br/><em>mais contexto.</em></h1><p>Roteiros, escolhas práticas e histórias para transformar planejamento em parte da viagem.</p></section><section className="blog-feature section"><Link href={`/blog/${featured.slug}`} className="featured-post"><img src={featured.image} alt=""/><div><p className="kicker">{featured.country} · {featured.category}</p><h2>{featured.title}</h2><p>{featured.excerpt}</p><span>Ler guia · {featured.readTime} →</span></div></Link></section><section className="blog-list section"><div className="section-title"><div><p className="kicker">ÚLTIMOS GUIAS</p><h2>Ideias para o próximo caminho</h2></div></div><div className="post-grid">{rest.map(post=><Link className="post-card" href={`/blog/${post.slug}`} key={post.slug}><img src={post.image} alt=""/><div><p>{post.country} · {post.category}</p><h3>{post.title}</h3><span>{post.readTime} de leitura →</span></div></Link>)}</div></section></main><Footer/></>};
