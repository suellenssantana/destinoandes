import { notFound } from "next/navigation";
import Link from "next/link";
import { Footer, Header } from "../../components";
import { getPost, posts } from "../data";
export default async function PostPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=getPost(slug);if(!post)notFound();const related=posts.filter(x=>x.slug!==slug).slice(0,3);return <><Header/><main><article className="article"><header><Link href="/blog">← Voltar ao blog</Link><p className="kicker">{post.country} · {post.category}</p><h1>{post.title}</h1><p>{post.excerpt}</p><span>{post.readTime} de leitura</span></header><img className="article-cover" src={post.image} alt=""/><div className="article-body"><p className="article-intro">{post.intro}</p>{post.sections.map(section=><section key={section.title}><h2>{section.title}</h2><p>{section.body}</p></section>)}<aside><p className="kicker">PLANEJE COM QUEM CONHECE</p><h3>Quer transformar estas ideias em um roteiro?</h3><Link className="btn gold" href="/contato">Falar com especialista</Link></aside></div></article><section className="related-posts section"><div className="section-title"><h2>Continue planejando</h2></div><div className="post-grid">{related.map(x=><Link className="post-card" href={`/blog/${x.slug}`} key={x.slug}><img src={x.image} alt=""/><div><p>{x.country}</p><h3>{x.title}</h3><span>Ler →</span></div></Link>)}</div></section></main><Footer/></>};
